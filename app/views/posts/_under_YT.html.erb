<% if user_signed_in? && current_user.id == video.user_id %>
  <div class="menu-trigger" href="#">
    <li>
      <!-- 編集ボタン -->
      <%= link_to image_tag('edit.png', { :border => '0', :alt => 'EDIT', :size => "25x25"}), "/posts/#{video.id}/edit", method: :get %>
    </li>
    <li>
      <!-- 削除ボタン -->
      <%= link_to image_tag('delete.png', { :border => '0', :alt => 'DELETE', :size => "25x25"}), "/posts/#{video.id}", method: :delete %>
    </li>
    <li>
      
    </li>
  </div>
<% end %>
<div class="comment_open">
  <% if current_user %>
    <a class="comment_<%= video.id %>"><%= image_tag ("comment.png"), :size => '25x25' %></a>
    <div class="comment_box clearfix"> 
      <% if current_user %>
        <%= form_tag("/posts/#{video.id}/comments", method: :post) do %>
          <textarea class="comment_textarea" cols="30" name="comment" placeholder="Comment" rows="2"></textarea>
          <input class="comment_submit" type="submit" value="COMMENT">
        <% end %>
      <% end %>
    </div>
  <% end %>
</div>

<div class="comments">
  <% if video.comments %>
    <!-- <h4>＜COMMENTS＞</h4> -->
    <% video.comments.each do |comment| %>
      <p>
        <strong><%= link_to comment.user.username, "/users/#{comment.user_id}" %>：</strong>
        <%= comment.comment %>
      </p>
    <% end %>
  <% end %>
</div>

<div id="product-modal" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true"></div>

